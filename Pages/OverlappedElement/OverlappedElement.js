const {By, Builder} = require('selenium-webdriver');
const Page = require('../Page')
const {overlappedElementDict} = require('../dictionary')

const overlappedElementElements = {

}

class OverlappedElement extends Page {
    constructor(browser) {
        super(browser);
    }
}
