const {By, Builder} = require('selenium-webdriver');
const Page = require('../Page')
const {verifyTextDict} = require('../dictionary')

const verifyTextElements = {

}

class VerifyText extends Page {
    constructor(browser) {
        super(browser);
    }
}
