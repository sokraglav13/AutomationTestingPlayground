const {By, Builder} = require('selenium-webdriver');
const Page = require('../Page')
const {scrollbarsDict} = require('../dictionary')

const scrollbarsElements = {

}

class Scrollbars extends Page {
    constructor(browser) {
        super(browser);
    }
}
